[package]
name = "physics_benchmark"
description = "Physics stress test with dynamic sphere spawning and auto-scaling to target FPS"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "physics", "tracing"] }
rand = "0.9"
