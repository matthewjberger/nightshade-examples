[package]
name = "sdf_text"
description = "Signed distance field text rendering for crisp text at any scale"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "tracing"] }

[package.metadata.gallery]
display_name = "SDF Text"
