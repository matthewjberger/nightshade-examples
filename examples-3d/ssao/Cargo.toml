[package]
name = "ssao"
description = "Screen-space ambient occlusion post-processing effect for realistic contact shadows"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui"] }

[package.metadata.gallery]
display_name = "SSAO"
