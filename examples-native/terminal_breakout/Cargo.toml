[package]
name = "terminal_breakout"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { path = "../../../nightshade/crates/nightshade", default-features = false, features = ["terminal"] }
