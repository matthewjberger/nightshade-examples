[package]
name = "cyberdust"
description = "Roguelike dungeon crawler with turn-based combat, procedural generation, and grid-based gameplay"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["tracing"] }
