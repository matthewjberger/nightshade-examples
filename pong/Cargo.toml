[package]
name = "pong"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"
default-run = "pong"
publish = false

[features]
default = []
openxr = ["nightshade/openxr"]

[dependencies]
nightshade = { version = "0.6.46", features = ["egui", "tracing"] }
