[package]
name = "lighthouse"
version = "0.0.0"
edition = "2024"
publish = false

[features]
default = ["terminal"]
terminal = ["nightshade/terminal"]
tui = ["nightshade/tui"]

[dependencies]
nightshade = { workspace = true }
serde = { version = "1", features = ["derive"] }
bincode = "1.3"
