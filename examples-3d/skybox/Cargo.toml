[package]
name = "skybox"
description = "HDR skybox rendering with equirectangular to cubemap conversion"
version = "0.6.7"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
nightshade = { workspace = true, features = ["egui", "tracing"] }

[package.metadata.gallery]
display_name = "HDR Skybox"
