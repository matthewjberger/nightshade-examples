[package]
name = "multi_world"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["tracing", "physics", "egui", "gamepad"] }

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
wry = "0.54"
uuid = { version = "1", features = ["v4"] }
