[package]
name = "reactive_ui"
description = "Minimal reactive UI demo using named properties and event reactions"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["tracing"] }

[package.metadata.gallery]
display_name = "Reactive UI"
