[package]
name = "survivors"
description = "Vampire Survivors-style action roguelike with upgrades, enemy waves, and particle effects"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["gamepad", "tracing"] }
