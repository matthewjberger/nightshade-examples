[package]
name = "navmesh"
description = "Navigation mesh pathfinding with A* search, funnel smoothing, and agent avoidance"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "tracing", "navmesh", "physics"] }

[package.metadata.gallery]
display_name = "NavMesh"
