[package]
name = "depth_of_field"
description = "Cinematic depth of field post-processing with bokeh, adjustable focus distance, and CoC visualization"
version = "0.1.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui"] }

[package.metadata.gallery]
display_name = "Depth of Field"
