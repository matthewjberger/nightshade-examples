[package]
name = "farming"
description = "Farming simulation with time progression, crop growth, NPCs, and dialogue system"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["gamepad", "tracing", "fbx"] }
