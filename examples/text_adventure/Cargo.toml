[package]
name = "text_adventure"
description = "Interactive fiction game with inventory system and puzzle mechanics"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["tracing"] }
stateless = "0.1.0"
