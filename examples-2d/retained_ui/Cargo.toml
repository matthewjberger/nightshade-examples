[package]
name = "retained_ui"
description = "Retained layout UI system with animated state transitions"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["tracing"] }

[package.metadata.gallery]
display_name = "Retained UI"
