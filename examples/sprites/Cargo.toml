[package]
name = "sprites"
description = "2D sprite rendering with layered rendering, animation, and bouncing physics"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "tracing"] }
image = "0.25"
