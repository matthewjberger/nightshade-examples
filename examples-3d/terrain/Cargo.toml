[package]
name = "terrain"
description = "Infinite procedural terrain with GPU compute tessellation, distance-based LOD, and wireframe debug view"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "physics", "tracing"] }
bytemuck = "1.24.0"
