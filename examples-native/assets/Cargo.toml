[package]
name = "assets"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "tracing", "fbx"] }
image = "0.25"
serde_json = "1"
gltf-json = "1.4"
rfd = "0.15"
