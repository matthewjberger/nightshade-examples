[package]
name = "voxels"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[features]
openxr = ["nightshade/openxr"]

[dependencies]
nightshade = { workspace = true, features = ["egui", "tracing"] }
noise = "0.9"
