[package]
name = "speedreader"
description = "RSVP speed reading with per-character coloring and anchor point positioning"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["tracing"] }

[package.metadata.gallery]
display_name = "Speed Reader"
