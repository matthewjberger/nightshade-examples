[package]
name = "block_breaker"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"
default-run = "block_breaker"
publish = false

[features]
default = []
openxr = ["nightshade/openxr"]

[dependencies]
nightshade = { version = "0.6.46", features = ["physics", "tracy", "gamepad"] }
