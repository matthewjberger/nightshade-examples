[package]
name = "navmesh"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"
default-run = "navmesh"
publish = false

[dependencies]
nightshade = { workspace = true, features = ["egui", "tracing", "navmesh", "physics"] }
tracing = "0.1"
