[package]
name = "tui_breakout"
description = "Terminal-based brick-breaking game with ASCII rendering"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["tui"] }

[package.metadata.gallery]
display_name = "TUI Breakout"
