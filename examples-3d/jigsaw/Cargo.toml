[package]
name = "jigsaw"
description = "Interactive jigsaw puzzle with procedural piece generation, snapping, and rotation"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "tracing", "physics"] }

[package.metadata.gallery]
display_name = "Jigsaw Puzzle"
