[package]
name = "audio"
description = "3D spatial audio with distance-based filtering and listener positioning"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[features]
openxr = ["nightshade/openxr"]

[dependencies]
nightshade = { workspace = true, features = ["tracing", "audio"] }
