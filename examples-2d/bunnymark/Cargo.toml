[package]
name = "bunnymark"
description = "Sprite rendering benchmark with adaptive performance targeting"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "physics", "tracing"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
