[package]
name = "decals"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"
publish = false

[dependencies]
nightshade = { version = "0.6.46", features = ["tracing", "egui"] }
tracing = "0.1"
rand = "0.9"
image = { version = "0.25", default-features = false, features = ["png", "hdr"] }
