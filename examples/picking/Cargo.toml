[package]
name = "picking"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"
default-run = "picking"
publish = false

[dependencies]
nightshade = { workspace = true, features = ["egui", "physics", "tracing"] }
