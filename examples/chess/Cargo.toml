[package]
name = "chess"
description = "Interactive 3D chess game with piece selection, movement, and board interaction"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["physics", "tracing"] }
