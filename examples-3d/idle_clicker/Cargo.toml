[package]
name = "idle_clicker"
description = "3D idle clicker game with upgrades and visual progression"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["tracing"] }

[package.metadata.gallery]
display_name = "Idle Clicker"
