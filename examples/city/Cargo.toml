[package]
name = "city"
description = "Procedurally generated city with first-person exploration and day/night cycles"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "gamepad", "physics"] }
noise = "0.9"
rand = "0.9"
