[package]
name = "picking"
description = "3D object picking with raycasting"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "physics", "tracing"] }

[package.metadata.gallery]
display_name = "3D Picking"
