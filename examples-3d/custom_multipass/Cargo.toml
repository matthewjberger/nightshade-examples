[package]
name = "custom_multipass"
description = "Chaining multiple custom render passes"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "tracing"] }
