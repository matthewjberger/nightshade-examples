[package]
name = "roguelike"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"
default-run = "roguelike"
publish = false

[dependencies]
nightshade = { version = "0.6.46", features = ["tracing"] }
