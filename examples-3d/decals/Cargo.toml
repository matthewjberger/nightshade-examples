[package]
name = "decals"
description = "Deferred decal projection system with GPU depth picking for placing decals on arbitrary surfaces"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["tracing", "egui"] }
