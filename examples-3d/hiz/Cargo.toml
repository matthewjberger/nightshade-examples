[package]
name = "hiz"
description = "Infinite procedural grid demonstrating hi-z occlusion culling with dynamic chunk streaming"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["egui", "gamepad"] }

[package.metadata.gallery]
display_name = "Hi-Z Occlusion"
