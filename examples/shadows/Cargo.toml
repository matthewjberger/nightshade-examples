[package]
name = "shadows"
description = "Dynamic shadow mapping with directional lights and PCF filtering"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
nightshade = { workspace = true, features = ["tracing"] }
