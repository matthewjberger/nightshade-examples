[package]
name = "ui"
description = "Immediate-mode UI system with panels, widgets, theming, and docking"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["tracing"] }

[package.metadata.gallery]
display_name = "UI Demo"
