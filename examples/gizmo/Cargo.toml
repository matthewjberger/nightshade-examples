[package]
name = "gizmo"
description = "Interactive 3D transformation gizmos for translation, rotation, and scale"
version = "0.1.0"
license = "MIT OR Apache-2.0"
edition = "2021"

[dependencies]
nightshade = { workspace = true, features = ["egui", "tracing", "physics"] }
