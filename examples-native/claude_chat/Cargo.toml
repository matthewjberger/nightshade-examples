[package]
name = "claude_chat"
description = "Chat UI using nightshade's Claude CLI wrapper with egui"
version = "0.1.0"
authors = ["Matthew Berger <matthewjordanberger@gmail.com>"]
edition = "2024"

[dependencies]
nightshade = { workspace = true, features = ["claude", "egui"] }
serde_json = "1"
